# Production Environment Variables
# This file is used when building for production (npm run build)

# API Configuration
# Backend API base URL
# If your Django backend uses /api prefix, use: https://api.tailoredpsychology.com.au/api
# If not, use: https://api.tailoredpsychology.com.au
VITE_API_BASE_URL=https://api.tailoredpsychology.com.au/api

# Environment
VITE_ENV=production

# App Name
VITE_APP_NAME=Clink Psychology Clinic

# Debug (should be false in production)
VITE_DEBUG=false
